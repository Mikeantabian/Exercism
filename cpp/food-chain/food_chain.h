#if !defined(FOOD_CHAIN_H)
#define FOOD_CHAIN_H
#include <string>
using std::string;

namespace food_chain {
    string verse(int line);
    string verses(int line1, int line2);
    string sing();
}  // namespace food_chain

#endif // FOOD_CHAIN_H